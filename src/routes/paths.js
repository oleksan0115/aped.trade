// ----------------------------------------------------------------------

// function path(root, sublink) {
//   return `${root}${sublink}`;
// }

const ROOTS_HOME = '/';

const ROOTS_DASHBOARD = '/dashboard';

// ----------------------------------------------------------------------

export const PATH_DASHBOARD = {
  root: ROOTS_DASHBOARD
};

export const PATH_PAGE = {
  root: ROOTS_HOME,
  tradingPage: '/trading-view',
  contactUs: '/contact-us'
};
